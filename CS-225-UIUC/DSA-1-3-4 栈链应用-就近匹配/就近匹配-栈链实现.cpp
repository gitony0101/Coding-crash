// #define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
using namespace std;
/*就近匹配 检测括号是否匹配
- 扫描字符串，如果碰到左括号，直接入栈
- 如果碰到右括号，从栈顶弹出扩号元素，判断是否是左括号
  - 如果是，匹配成功
  - 如果不是，匹配失败
*/
#include "LinkStack.h" //引入头文件

void myFunc() {}

int main() {

  printf("\n");
  return 0;
}
